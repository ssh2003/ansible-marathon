clone:
  default:
    image: plugins/git
    depth: 50
    recursive: false
    
pipeline:
  resolve:
    image: alpine
    commands:
      - cat /etc/resolv.conf
      - echo "BBBBBB"
    when:
      status: [ success, failure ]
